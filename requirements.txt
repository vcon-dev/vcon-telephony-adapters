# Dependencies are declared in pyproject.toml (the single source of truth).
# Install the package with its runtime deps:
#   pip install .
# Add the S3 media publisher and/or dev/test tooling as needed:
#   pip install ".[s3]"
#   pip install ".[dev]"
#   pip install ".[all]"
-e .
